{
  "name": "ec2empty",
  "version": "0.1.0",
  "bin": {
    "ec2empty": "bin/ec2empty.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.104.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.104.0",
    "constructs": "^10.0.0"
  }
}
